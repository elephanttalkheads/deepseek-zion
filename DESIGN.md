# DESIGN.md — 项目级视觉设计规范

> 状态:Active / 项目级视觉单一事实源
>
> 风格:**终端(TUI)+ Matrix + 极简**
>
> 沉淀纪律:本文每条内容都经 demo 实践 + 用户确认后写入。第 4 章「待验证区」的候选在确认前不作为执行依据;确认后移入第 2 章。

## 1. 创作指令

**第一命题:界面是一台正在工作的终端,不是套了绿色皮肤的 SaaS。**

Matrix 的辨识度来自黑场、磷光绿、字形与状态叙事,不来自效果堆叠。极简靠**少放元素**达成,不靠全局调淡;信息可以密,但分区必须稳定、边线必须统一、同屏运动源 ≤1。

**TUI 四成分(已裁决的方向,参照系 = claude code / kimi code 一类终端 agent UI):**

1. **全局等宽** — UI 文案、控件、菜单一律等宽字体;中文走字体回退链。
2. **提示符语义** — 输入行以 `❯` 开头;系统输出带前缀/状态符,读起来像终端会话。
3. **框线字符分隔** — 分区用 1px 发丝线与框线字符,不用卡片/阴影/圆角堆叠。
4. **文本即界面** — 状态用文字与符号表达(`READY` / `2x` / `▮▮▮░░ 51%`),图标让位给字符。

外加半条:**焦点态可见** — `:focus-visible` 有明确指示,键盘路径全程可走;但不要求快捷键提示常驻(常驻 hint 抬密度,违背极简)。

**与既有样式的关系**:与本文冲突的既有样式均为待修正,按用户点名的优先级逐个走「demo → 确认 → 落地」重刷,不做一次性全量重写。

## 2. 已定锚点

三个锚点是新规范的种子:已实践、已确认,参数从现有实现**原样提取**固化,改动需经用户确认。

### 2.1 数字雨背景(已落地,形态基准 = 当前实现)

实现:`renderer/src/ui/RainCanvas.tsx` + `renderer/src/styles/ambient.css` + `renderer/src/app/ambient-fx.ts` + `renderer/src/matrixGlyphs.ts`。

- **挂载**:`<canvas id="rain" aria-hidden>`,AppFrame 内第一个子节点;`position: fixed; inset: 0; z-index: -1`,永远排在所有 UI 之下。
- **字符集**:全角片假名 34 字 + 数字 `012345789`(无 6)+ `*+<>:|`,单一事实源 `matrixGlyphs.ts`,不得另写字符集。
- **字形**:字体 `"Matrix Code", "Share Tech Mono", monospace`;字号/列宽 `FS = 18px`;字形横向压缩 `GLYPH_SX = 0.55`(恢复原半角观感,网格不变)。
- **运动**:每帧下落 `FS * 0.9`;帧节流 `90ms / fx.speed`(READY 档 ≈11fps,忙碌档 ≈24fps);拖尾 = 每帧盖 `rgba(1,10,4,0.035)`。
- **色彩**:普通字形 `rgba(61,255,143,0.95)`(磷光绿);12% 亮头 `rgba(220,255,232,1)` + 辉光 `rgba(120,255,175,0.9) blur 8`。
- **呼吸**:速度两档(非连续插值)——READY `{speed:1}` / 忙碌 `{speed:2.2}`,由选中会话 running 快照驱动;FX 状态走模块级对象,**不进 React 渲染路径**。
- **稀疏**:列落出屏底后仅 3.5% 概率重生,保证大块黑色留白。
- **reduced-motion**:只绘制一帧静态雨幕(`rgba(61,255,143,0.6)`,隔列布字),不起 rAF。
- **配套 CRT 扫描线层** `.scanlines`:`fixed; z-index: 40; pointer-events: none`,1px/4px 横向暗纹 `rgba(0,0,0,0.10)`。
- **压暗靠遮蔽,不靠调雨**:雨幕本身永远按上表全参数绘制;各分区底刷 `rgba(2,18,9,0.92)`(真仓 `--bg-panel`),雨只在缝隙/空态以全亮度出现。信息层与氛围层靠遮蔽分工,任何界面不得用「降低雨的参数」来换可读性。

### 2.2 全局滚动条(已落地,`renderer/src/styles/tokens.css:103-107`)

```css
*::-webkit-scrollbar { width: 6px; height: 6px; }
*::-webkit-scrollbar-track { background: transparent; }
*::-webkit-scrollbar-thumb { background: #00ff66; border-radius: 3px; }
*::-webkit-scrollbar-thumb:hover { background: #66ff99; }
*::-webkit-scrollbar-corner { background: transparent; }
```

6px 终端绿胶囊,轨道/角落透明;通配选择器全局生效,新滚动容器自动同款。

### 2.3 输入栏 InputBar(形态基准 = `ui-prototype/input-bar/input-bar-proto.html`)

结构(自下而上):dock 停靠排(TodoDock / GoalBar / QueueDock / 插件卡)→ 微簇状态条 `.micro`(权限 chip、Plan chip | 模型菜单、ctx 胶囊条+百分比、推理等级、状态)→ 发丝边框输入壳(附件 rail + 输入行:「+」命令、📎 附件、`❯` 提示符、textarea、停止 ✕、发送 ↑)→ 统计条 StatsLine。弹层:命令面板 `.palette`/+ 面板 `.cmd-panel`/模型菜单 `.model-menu`/风险确认 `.risk-mask`/拖放 `.drop-overlay`/灯箱 `.lightbox`。

**设计令牌**(`:root`):

| 令牌 | 值 | 角色 |
|---|---|---|
| `--bg` | `#010a04` | 黑场底 |
| `--surface` / `--surface-2` | `rgba(2,18,9,0.92)` / `rgba(3,26,13,0.94)` | 分区底 |
| `--text-primary` | `#3dff8f` | 主文本(磷光绿) |
| `--text-secondary` / `--text-tertiary` | `#23c468` / `#1da754` | 次级/弱提示 |
| `--accent` | `#00ff41` | 活跃/可交互 |
| `--bright` | `#c2ffd9` | 峰值/焦点(全屏克制使用) |
| `--warning` / `--danger` | `#ffb000` / `#ff5555` | 警告/危险(仅语义位) |
| `--border` | `rgba(61,255,143,0.18)` | 1px 发丝线 |
| `--font` | `"Share Tech Mono", ui-monospace, "Cascadia Mono", "Courier New", "Sarasa Term SC", "Microsoft YaHei", monospace` | 全局等宽 + 中文回退 |

**度量基调**:正文/输入 15px;状态/dock 12px;chip/统计 11px;数字一律 `tabular-nums`;输入框行高 22.5px、5 行封顶;分隔只有 1px `var(--border)` 发丝线,`:focus-within` 底边转 `--accent`;`:focus-visible` outline 1px accent offset 2px。

**z-index 分层**:`#rain` -1 / 面板类 30–35 / `.scanlines` 40 / `.drop-overlay` 80 / `.risk-mask` 85 / `.lightbox` 90。

### 2.4 亮度四阶分层(C11)

全局层次收敛为单一 `#3dff8f` 的四档不透明度,档数封四——需要第五档时重新设计,而不是加档。

| 档 | 值 | 合成 ≈ | 用途 |
|---|---|---|---|
| 100 | `rgba(61,255,143,1)` | `#3dff8f` | 正文 / 主状态 |
| 70 | `rgba(61,255,143,.7)` | `#2bb565` | 次要文本 / 工具行 / 完成态 |
| 40 | `rgba(61,255,143,.4)` | `#196c3c` | 边框 / 辅助信息 / 空闲态 |
| 20 | `rgba(61,255,143,.2)` | `#0d3b20` | 发丝线 / 水印 / 禁用 |

层次问题不出单色,唯一例外是语义红(见 2.5)。既有异色阶梯(`#23c468` / `#1da754` 等独立 hex)为**待修正**,逐步归并到 70/40 档,不再新增异色阶。

> 验证:`ui-prototype/luminance-tiers/`(微 demo,2026-08-29 确认)+ `ui-prototype/composite-tui/`(综合,2026-08-29 确认)。

### 2.5 字形舱位状态图标(C12 · SET D)

状态 = 图标 + 亮度档,不用颜色。五枚闭集,全部是「字形舱位」(glyph cell)——状态 = 舱位里发生的事:

| 态 | 图标 | 档 |
|---|---|---|
| 运行 | Matrix Code 字形扰码循环(120ms 随机换字;与数字雨同族,**不另计运动源**) | 100 |
| 等待 | 沙漏(发丝线,上半舱填充 = 剩余) | 70 |
| 空闲 | 空舱(发丝方框) | 40 |
| 完成 | 锁定舱 + 发丝勾 | 70 |
| 错误 | 故障切片(同一字形上下半错位),`#ff5555` | 语义红 |

- **唯一语义红**:错误/拒绝是全屏唯一红色语义位(工具失败、审批拒绝、连接断开共用),不再另设橙/黄/紫。
- 落地形态 = 内联 SVG 原子组件,`currentColor` 继承 2.4 亮度档;Matrix Code 字体随仓打包,扰码由 JS 驱动 textContent。**不引入图标字体 / Nerd Font**。
- 新增状态先进 2.4/2.5 的闭集评估,不得就地发明第六枚。

> 验证:`ui-prototype/status-icons/`(SET D,2026-08-29 确认;沙漏/锁定勾为当轮迭代确认)。

### 2.6 节点前缀符号体系(C01)

对话流层级用前缀符号与缩进承担,不用气泡/卡片/底色块:

- **用户消息** = `❯` 前缀 + 6% 磷光薄底(与输入栏提示符同形);多行续行对齐文本起点,不重复前缀。
- **AI 正文** = 无前缀、100% 档、无框无底——可读性是正文的唯一装饰。
- **工具结果 / 补充说明** = `⎿` 缩进续行挂父行下,字号降半档、70% 档;层级 = 缩进深度,不是嵌套卡片。

被替代物:消息气泡、工具卡底色块、kind 侧色条(既有样式属待修正)。

> 验证:`ui-prototype/node-prefix/`(微 demo,2026-08-29 确认)+ `ui-prototype/composite-tui/`(综合,2026-08-29 确认)。

### 2.7 思考块磁带纹(流式 V3 突发 + 闭环 V1 慢波)

思考块的状态动效 = 磁带纹横轨,与雨同族(Matrix 绿 + 拖尾),是思考相位唯一的动效位。

- **轨道 DNA(不变)**:14px 横轨;竖划 5px 间距、厚 1.4px、高 3–8px、y 向抖动 2–6;60ms 帧;`destination-out rgba(0,0,0,0.10)` 拖尾;绿 `rgba(0,255,65,0.4)`,白热头 `rgba(194,255,217,0.8)`。
- **流式(思考中)= 段落突发(V3)**:常时漂移 1.3px/帧;每 1.2–2.5s 一次写脉冲——整段 3 倍速(6.6px/帧)+ 前沿一道白热头(一次一道),脉冲 ~0.54s。漂移 = 酝酿,脉冲 = 落笔。
- **闭环(已沉降)= 亮度慢波(V1)**:竖划固定,亮度波 1.3px/帧(一波 ~12s)扫过栅格,σ 22px,峰值封顶 55%、无白热——呼吸即沉降,与流式行同框时自然让位。
- **reduced-motion / 历史回放** = 静态冻结帧 `rgba(20,184,80,0.28)`。

> 验证:`ui-prototype/tape-variants/`(V0–V3 对照 + 组合节,2026-08-29 确认;慢波速度经一轮「不可察 → 1/2 速」迭代)。

### 2.8 工具纯行 + 角括号聚光(D2 + V2)

工具调用的默认形态 = C01 纯行(图标 + 工具名 + `⎿` 续行),**无框**。框不是容器,是状态信号,只授予正在发生的事:

- **运行中** = 角括号聚光:四角 `⌜ ⌝ ⌞ ⌟`(70% 档)+ notch 标题行(`┐ title ┌`)+ 扰码图标;四边不画线。闭环(完成/失败)角标即消,工具回到纯行。
- **完成** = 纯行,SET D 锁定勾图标,70% 档。
- **错误** = 红图标 + 红文字行(唯一语义红),**不画红框**——语义红小而准。
- 角括号是「系统正在注视此处」的通用语言(审批/菜单聚焦共用同一符号),但每次只服务一个焦点。
- 内容折叠/展开为整行点击(与官方 toggle 语义一致),展开内容继续 `⎿` 缩进。
- 被替代物:一工具一框(2026-08-29 迭代裁决:框线密度爆炸/空心框/红框过吵)、工具卡底色块、左侧色条、圆角。

> 验证:`ui-prototype/tool-card/`(全框 A/B)→ `ui-prototype/tool-density/`(D2 vs D3)→ `ui-prototype/live-frame/`(V1 活框 vs V2 角括号,2026-08-29 选 V2)。

### 2.9 回合不分隔(C06 修订)

回合(一次 user → assistant → tools 闭环)之间**不画任何分隔线**,靠 ❯ 话头行与自然间距分组。

- 每轮回复底部也不放汇总 meta 行:耗时/token 信息由回复尾操作条的 meta(§2.14)与 composer 状态行承担,一处信息只出现一次。
- 被替代物:全宽 1px 回合发丝缝(C06 初轮选 A,引入回复尾操作条后线与条功能重叠、行间变挤,2026-08-29 修订废止);居中 ASCII 虚线(初轮已否决);每轮回复下的汇总 meta 行(与操作条 meta 重复,一并废止)。

> 验证:`ui-prototype/turn-divider/`(初轮 A 发丝线 vs B ASCII 虚线);2026-08-29 用户裁决:去缝去 meta,composite 已同步。

### 2.10 反色选中行(C08)

列表(侧栏/队列/菜单/一切可枚举行)的选中态 = 整行反显:底 = 100% 磷光 `#3dff8f`,字 = 黑场 `#010a04`。

- 选中 = 列表的光标:最高对比,不加边框、不加左侧色条;行内图标/文字/辅助信息一并反色。
- hover 与选中分两档:hover = 6% 磷光薄底,选中 = 反显,二者必须一眼可分。
- 错误行被选中时红让位给反显,语义由 ✗ 图标保留。
- 反色行是列表里唯一允许的大面积高亮;一个列表一屏至多一个反色行。
- 被替代物:选中态的底色块 + 左边条组合(既有样式属待修正)。

> 验证:`ui-prototype/row-select/`(A 全反 vs B 柔反对照,2026-08-29 选 A)。

### 2.11 审批弹层:角括号注视 + 药丸三选(C07 + C15 场景化定案)

审批 = 系统停下等一个决定。面板接管 composer 位(与官方 ApprovalPanel 语义一致):

- **面板** = 四角角括号 `⌜ ⌝ ⌞ ⌟`(注视),与运行工具的角括号同族但语义分层:工具 = 正在发生,审批 = 正在等你。
- **选项 = 三颗双纹半囊药丸**(严格 8:8 两半、无缝:左半纯亮平涂,右半 1.5px 密颗粒强抖——字符块肌理,与上下文条 █/░ 同语汇),置于选项开头、取代数字标记:
  - **蓝丸 = 允许**(亮 `#6db3ff` / 暗 `#1c5fb0`;第二色相,锁死审批场景,不外溢)
  - **绿丸 = 本次会话不再询问**(亮 `#8fffc0` / 暗 `#14b850`;磷光绿 = 系统本色,语义 = 信任/被系统记住)
  - **红丸 = 拒绝**(亮 `#ff8a8a` / 暗 `#b01c1c`;回归语义红,与错误/拒绝全系统一致)
- **键盘**:`1/2/3` 直选(蓝/绿/红),`←→` 移焦点,`enter` 确认,`esc` = 拒绝(与官方语义一致,不得静默放行);foot 行(40% 档)记录键位。
- 焦点 = 选项段反显(C08 光标的紧凑变体);高危命令在请求行右端以文字标注(如「高危:递归删除」),不另设颜色。

> 验证:`ui-prototype/approval/`(A 横排 / B 竖排 / C 药丸)+ `ui-prototype/pill-variants/`(第一轮 V1–V4 选双色半囊;第三轮字符块肌理 V1 双纹半囊迭代:去中缝 → 8:8 精确半格 → 亮半去颗粒/暗半 1.5px 密颗粒,2026-08-29 定稿)。

### 2.12 上下文进度条:分段小胶囊(V1)

上下文占用条 = 一串分段小胶囊,与滚动条/审批药丸同一条「平涂胶囊」血统:

- **格** = 平涂小胶囊(圆角 = 半高):占用格 = 100% 磷光,空格 = 20% 档(与轨道纪律一致),零渐变零描边零辉光。
- **格数语义**:全尺寸 26 格(终端方块条的格数遗产);微簇紧凑位 16 格(宽 ≈ 64px,格 3×7、间隙 1px)。占用按比例点亮,四舍五入。
- **读数**:`Context:` 标签与用量(如 `142k/1.0M` 或 `51%`)走 40–70% 档、tabular-nums;条与读数同行。
- **高位不变色**:占用高不是错误,不走语义红;需要强调时只升到 100% 档。
- 被替代物:微簇里的 56px 单胶囊渐变条(既有多色异轨,属待修正);█/░ 字符方块条(V0 复刻案,辨识度高但与胶囊血统不同族,否)。

> 验证:`ui-prototype/context-bar/`(V0 1:1 字符复刻 / V1 药丸分段 / V2 发丝刻度,2026-08-29 选 V1)。

### 2.13 面板标题:单线嵌题(C09 选 B)

面板(侧栏会话/队列 dock 等)的标题嵌进一条顶发丝线,不占内容行、不围框:

- 结构 = `─ 标题 (n) ────────── 序号/总数 ─`:左短衬线 + 标题(70% 档)+ 条数(40% 档)+ 中段发丝填充线(20% 档)+ 右端位置指示(40% 档,tabular-nums)+ 右短衬线。
- **位置指示** = 选中项序号/总数(如 `3/12`),跟随选中变化。
- 标题行是面板唯一允许的「框」;内容区不画左右/底边,多面板并排靠间距分组。
- 被替代物:四边封闭框嵌题(lazygit 直译,框线总量翻倍,否);占内容行的面板标题(既有样式属待修正)。

> 验证:`ui-prototype/panel-title/`(A 全框嵌题 vs B 单线嵌题,2026-08-29 选 B)。

### 2.14 回复尾操作条:复制 / 赞 / 踩 / 分支 + meta(C10 修订二定案)

对话流的操作条**固定在每轮已结束回复的底部**(对齐官方 `actions` 条;进行中的回复不显示):

- **操作集** = 复制 / 好的回答(👍,messageFeedback)/ 有问题的回答(👎)/ 分支(`forkSession`)——与官方一致,不发明新操作。
- **档位** = 图标 40% 档常显,hover 升 100%;已赞/已踩 = 激活态 100% 档。
- **meta 随行** = `10:12 · 用时 3分30秒 · 首 token 61秒 · 117 tok/s`,40% 档、tabular-nums,跟在图标组后(官方布局)。
- **图标**(SET D 舱位语言:空心方块/细线、1px、方角、currentColor):复制 = 前后叠块;分支 = 竖干 + 直角横枝 + 三枚空心方块节点;赞/踩 = 直角细线拇指(官方 👍👎 语义直译)。
- 被替代物:消息级 hover 行尾显形(修订一,官方实为固定条,否);块级缝尾嵌字(初轮 B 案,否);「重跑」(官方没有,否);舱内 ± 赞踩(丢拇指通识性,否);meta 贴行右端(宽窗下离操作远,否)。

> 验证:`ui-prototype/round-block/`(块级 A/B)+ `ui-prototype/msg-action-icons/`(图标 V1 直角细线)+ `ui-prototype/msg-action-icons/reply-actions-proto.html`(回复尾条 V1 拇指随行 / V2 舱内± / V3 meta 贴右,2026-08-29 选 V1)。

### 2.15 流尾光标:字形蛾(C03 选 B)

流式输出的文本尾 = **字形蛾**:Matrix Code 随机字形,120ms 扰码切换 + 1.1s 呼吸(0.55↔1),70% 档;流毕即消失。

- 与数字雨/运行扰码同一字体、同一 120ms——三者读作**一族**而非三个运动源(这是「同屏运动源 ≤1」纪律在同族同频下的豁免:血脉相同,不互相争)。
- reduced-motion = 静态 `▌`,不闪不换字形。
- 被替代物:`▌` 块光标 500ms 闪烁(终端传统,静但与雨/扰码不同族,否)。

> 验证:`ui-prototype/stream-caret/`(A ▌ 块光标 vs B 字形蛾,2026-08-29 选 B)。

## 3. 不可交换的边界

风格可以重做,以下边界不因任何风格任务豁免:

- 展示真实事件、任务、文件、耗时和结果。未知数据明确标记为未知,不生成假进度、假 token、假安全结论或假工具调用。
- 对外只呈现可交付的 reasoning 摘要、计划、动作和证据,不暴露隐藏思维链、凭据、系统提示或敏感环境信息。
- 提交、删除、终止、重试、项目切换等关键操作始终有可理解的文字、键盘路径、焦点状态和必要确认。
- 颜色、声音和动画均有语义等价物;主要交互具有正确的原生语义或 ARIA。
- 遵守闪烁安全,提供暂停/静音/reduced-motion 路径:降级直接显示终态,无拉伸、无中间帧闪现,且错误时仍可操作。

## 4. 待验证区

(空)

候选设计语言入区流程:research 精选 → 单元素微 demo → 用户确认 → 综合场景 demo 验证搭配 → 用户肯定 → 移入第 2 章。demo 与验证截图放 `ui-prototype/<主题>/`。
