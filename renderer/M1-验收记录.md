# M1 验收记录 — 三栏骨架 + 会话列表(fixture)

日期/轮:goal-313d12cb round 2。范围:Q3A/Q9A 的 M1 竖切(三栏 + 会话列表 CRUD/搜索 + 空态 + fixture +
插件运行时底座的设计口径)。Q5A 双通道对照。

## 1. 交付文件(renderer/)

```
renderer/
├─ index.html  vite.config.ts  tsconfig.json
├─ src/
│  ├─ main.tsx                 # entry: RuntimeProvider → AppFrame
│  ├─ styles.css               # 深色中性骨架(props 对齐官方:浅色/logo 待 M1 收尾精修)
│  ├─ app/runtime.tsx          # RuntimeProvider + useSessions(bindSnapshotSelector) + 选中/连接态
│  ├─ protocol/assemble.ts     # B 直拼:pickApi(fixture/WebApiClient) → ConnectionController → SessionManager
│  │                           #  mux/host 帧泵给 manager.handleMuxEnvelope / handleConnected / handleDisconnected
│  └─ ui/
│     ├─ AppFrame.tsx          # 三栏 + 顶栏(品牌/新会话/工作区/fixture badge)
│     ├─ Sidebar.tsx           # 会话列表:深度缩进/搜索/运行徽标/相对时间/workspace名/选中高亮
│     ├─ ConversationDock.tsx  # 中央:hero 空态(选中后 placeholder——M2 接流)
│     └─ DetailsPanel.tsx      # 右栏占位
└─ vendor/
   ├─ client-connection/       # 官方源码(纯类:WebApiClient/FixtureApiClient/ConnectionController)
   ├─ client-runtime/          # 官方源码(SessionManager/Session/ProjectionValueStore/assembler)
   ├─ client-web-react/        # bindSnapshotSelector(uSES bridge)
   └─ client-ui-slots/         # SlotCore/HostObservable 契约
```

依赖:官方 ESM 面包(file:`C:/Users/zyf/.dsh/profiles/node_modules/@deepseek-ai/*` @0.1.0-rc.6:
apiproxy/llm/session/commands/attachment/tools/session-projection/session-title/api-remotes/agent/
typert-protocol/llm-retry)+ `@deepseek-ai/cordis@4.0.1` + react/immer/zustand/use-sync-external-store。

- `npm run build:web` → 375 KB JS,1.1s;`npm run typecheck` → 0 错。
- Q8A 修正:file: 引 node_modules 的本地 rc.6 ESM 面(公共 npm rc.1 链缺 4 包);
  client 系四包是 ClientModuleSystem bundle(不可直 import),故 vendor 其源码(见本文 §3)。

## 2. 验收证据(双通道)

### 通道 A — 结构与功能(文本对照,官方 fixture vs 复刻 fixture)

| 区域 | 官方 fixture(3080/?fixture) | 复刻 fixture(preview/?fixture) | 对齐 |
|---|---|---|---|
| 顶栏 | 新会话 / 工作区 fixture / fixture 态标 | 品牌·新会话 / 工作区 fixture / FIXTURE | ✅ |
| 侧栏[1] | Fixture 历史会话 / 刚刚(父,running=进行中) | Fixture 历史会话 / 进行中 / 刚刚 | ✅ |
| 侧栏[2] | fixture / 1分钟(子,workspace名+时间) | fixture / 1分钟 | ✅ |
| 侧栏[3] | fixture / 2分钟 | fixture / 2分钟 | ✅ |
| 中央 | 探索未至之境 / 预览版 hero(受模态遮挡) | DeepSeek Harness hero 空态 | ⚠️ 需 M1 收尾对齐字面|
| 右栏 | 详情 / 点击工具行查看详情 | No selection 占位 | ⚠️ 需 M1 收尾 |
| JS 错误 | — | 0(console level>=3 空) | ✅ |

> 官方 fixture 首载弹出「内测声明」模态(点继续后因保存状态失败回弹),遮挡中央/右栏;本表以 body
> innerText + DOM 为准,像素对照受模态干扰,中央/右栏对齐归档为 ⚠️ 后补。

### 通道 B — 像素基线(vision_pixel_diff,1440×900 → 1424×835)

- **总体差异 72.77%**,最差区集中在中央对话区(composer/流式/hero)与详情区——这些是 M2/M3 尚未实现的区
  域,差异为**预期敞口**;侧栏/顶栏差异相对小。
- 证据:`zion-verify/`(官方 vs 复刻 .png/.txt)、`m1-pixel-diff/heatmap.png` + report.json。
- **基线使命**:M2/M3 每批交付后重跑同屏 diff,目标逐步收敛(中央区 → <10%)。

## 3. 关键架构事实(供后续批)

- **官方 client 包发布形态** = ClientModuleSystem bundle(`lib/client.js` = `window.__ModuleLoader__.load`),
  file: 直 import 会 `window is not defined`;故 Q16B 采用**vendor 源码 + Vite 直编**。
- **官方 ESM 面**(apiproxy/api, llm, session…)是普通 ESM,可直接 file: 依赖。
- **装配 = 纯类直拼** 已跑通:FixtureApiClient → ConnectionController → SessionManager 泵帧 →
  bindSnapshotSelector(useSessions) → React 树。全程无 cordis(插件运行时除外)。
- 官方 fixture 的会话标题/运行态经 `session/event` + `session/projection` 帧泵给 manager 的
  `handleMuxEnvelope` 后由列表投影呈现(补该接线后"Fixture 历史会话/进行中/刚刚"才出现)。

## 4. 遗留(M1 收尾 + 批内)

1. 插件运行时底座(最小自建,参考官方 cordis-client-runner client 面)——转 M1.5/M2 前置。
2. 中央 hero 字面与官方对齐(探索未至之境 / 预览版)+ 详情区结构。
3. 顶栏/主题浅色 1:1(官方偏浅色,现为深色中性)——M1 收尾或 M2。

## 5. 红线核对

- wire 契约零改动:未实现任何 RPC 重写,只消费官方 apiproxy/连接类。
- 会话语义不变:标题/运行/时间均来自官方 fixture 真实帧,无伪造。
- 不伪造遥测:侧栏数据 = manager 快照,全部来自 fixture/后端。

---

# M2 进展(appended,round 3)

## 交付
- 对话定义层:vendor `client-ui-conversation/src/client`(官方 conversation-nodes 12+1 kinds + chat-snapshot-builder view),经 `src/app/conversation.ts`(单一 cordis「UI 逻辑面」Context → ConversationEventRegistry/ViewRegistry → registerConversationNodes)挂给 SessionManager。
- 会话→对话装配线:RuntimeProvider 里 `useConversation` 按选中会话经 manager.get + session.open() + bindSnapshotSelector 绑 ConversationSnapshot。
- ChatView:按 kind 分派的自研文本渲染(Q19A 展示层),覆盖 user/steering/context/assistant/assistant-step/reasoning/tool-call/tool-result/command/compaction/turn-tail/model-retry/turn-error/turn-max-tokens/unknown。

## 验证(fixture,Electron 无头)
- 复刻:点击首会话后渲染 fixture 历史流(问题51+ / 回答51+ / 思考过程 / 工具调用 / 上下文注入 / turn complete),零 JS 错误 → `m2-chat2.txt` / `m2-chat2.png`。
- 侧栏文本对照官方:3 会话 + 相对时间 + 状态徽标一致。
- 像素全屏 diff 受限:官方 fixture 首屏「内测声明」模态在 Electron 无法关闭(保存状态失败 → 回弹),故 M2 以文本/结构对照为主,像素对等面留待真实后端(3080)验收。

## 修复记录
- vite alias 子路径被父前缀吞(`.../client` → `index.ts/client`):改正则锚定 alias。
- noSessionSource 返回 `undefined` 导致 `s.chat` 崩溃:改合法 EMPTY_CHAT_SNAPSHOT 空快照。
- 会话选择依赖 manager.get(懒建)+ open();mux 帧经 handleMuxEnvelope 泵入,投影/标题/时间到位。

## 遗留
- 对话区富渲染(样式/分色/工具树/steering 标记)达官方 1:1 —— M2 收尾。
- composer(输入区/模型/权限/队列 dock)—— M2 后半。
- 官方第三行「已完成」徽标在真实后端下校验(manager completed 由 running→false 边缘触发)。

---

# M3 进展(appended,round 4 — M3 剩余:审批/问卷/模型/工作区/附件)

## 方向
M3 剩余三项:审批/问卷(InteractionDock)、模型/工作区选择、附件限额。fixture 常驻 fx-alpha 审批+问卷帧,端到端可验证;模型选择经 selectModel + "report model" 回显闭环。

## 交付(都在 `renderer/src/`)
1. **tool-card 收尾** — `ToolCallCard.tsx`:`echo` 归 Bash(此前落 write)、`run_code/code` 变体 Title Case「Code」;用 `'kind' in block` 判别 RunningToolCall/ToolResultNode(修掉一整组类型误报)。
2. **runtime 扩展** — `app/runtime.tsx`:`models`(api.sessions.models 随选中会话拉取)、`selectModel`(selectModel + 重拉)、`workspaces`(workspace.list)、`imageLimits`(镜像附件投影常量:5MB/20张/100MB/4媒体类)、`sendPrompt` 改接 `PromptContentPart[]`(支持带图)。
3. **InteractionDock** — `ui/InteractionDock.tsx` 新:渲染 `snapshot.pending`(approval:工具名+原因+拒绝/允许一次;question:header/详情/选项/单选/多选(点选即归档,底部统一提交/取消)),应答走官方 `PendingWait.respond`(approval / question carrier)。
4. **QueueDock** — `ui/QueueDock.tsx` 新:渲染 `snapshot.queue`(queued/steering/context 摆放)+ `lastAgentError` 反馈条。fixture 不推 session/queue,此 dock 需真后端激活,组件就位。
5. **InputBar 增强** — 模型 `<select>`(按 provider 分组当前 current+groups)+ 图片附件(FileReader→base64,读后限额校验:类型/单张/张数/总字节限制,贴图/paste 支持,缩略图 chips+移除)。
6. **AppFrame** — 顶栏工作区名接真实 `workspaces[0].title`(fixture:「fixture」)。
7. **host 帧接线修复**(重要) — `protocol/assemble.ts`:此前 `onHostEnvelope` 只透传空 sink,未喂 `sessions.handleHostEnvelope` → `host/session-status` 丢失。修复后:停止能真的降 running、侧栏 completed(✓)徽标出现(顺带解决 M2 遗留「第三行已完成徽标」)。
8. 类型面:vendor-shims 补 `Context` value 声明;修 `ImageMediaType` 窄化。

## 验证(fixture,Electron 探针 `probe-m3.mjs`,证据 → `zion-verify/m3-interaction-*.png|txt`)
| 项 | 结果 |
|---|---|
| 审批卡渲染 | ✅ `工具需要授权 | dangerous_tool | fixture 常驻审批… | 拒绝 | 允许一次` |
| 审批应答结算 | ✅ 点「允许一次」→ approval/resolved → 卡片从 DOM 消失(零错误) |
| 问卷渲染 | ✅ 3 题(偏好/方式/信号)+ header/detail/选项 desc/多选标记全部渲染 |
| 问卷应答结算 | ✅ 三题选选项(点选即归档)→ 提交可点 → question/resolved → 卡片消失 |
| 模型选择器 | ✅ 渲染 DeepSeek-V4-Flash / -Pro / GPT-5(provider 分组) |
| 模型切换生效 | ✅ 选 openai/gpt-5 → 发「report model」→ fixture 回显 `当前模型：openai/gpt-5`(selectModel 真实写入) |
| 停止降 running | ✅ host/session-status 接线后「发送」按钮可现(此前永久「停止」) |
| 附件限额 UI | ✅ 附件按钮 + FileReader + 限额校验 + 缩略图 chips(体积/类型/数量) |
| 工具卡回归 | ✅ turn60 Bash(`ls -la echo done`)/ turn65 Code(Title Case) |
| JS 错误 | ✅ m3-console-errors.txt 空 |
| 视觉抽查 | ✅ vision:审批+问卷卡无重叠错位;输入栏被高内容挤到屏下(真后端/全屏对照待办) |

## 遗留
- 队列/steering dock 激活(需真后端 session/queue 帧;fixture 不支持)。
- 全屏像素 1:1 对照(官方 fixture 模态仍挡屏)→ 真后端(3080)验收。
- 模型/工作区富选择(工作区间切换/新建会话选工作区)、附件限额在真后端的投影验证。

---

# M4 进展(appended,round 5 — 队列真后端激活 + 插件运行时底座 + 真后端 24 项 + vision)

## 1. 真后端连通(3080)
- `renderer/vite.config.ts`:`/api` proxy → `http://127.0.0.1:3080`(changeOrigin + ws + **剥 Origin**)。
  3080 的 `api-request-trust` 要求 Host=loopback 且 Origin 同源或缺省;代理转发保留 5199
  Origin 会 403,故 proxyReq/proxyReqWs 剥 Origin。复刻页面(5199 不带 ?fixture)→ WebApiClient
  → 同源 /api → 转发 3080 → 真实会话/工作区/模型/工具卡全部落地。

## 2. 插件运行时底座(Q17A)
- 设计文档:`D:\pi-martix-ui\zion-verify\plugin-runtime-design.md`(子代理基于官方
  cordis-client-runner 源码逐文件蒸馏)。
- 实现(`renderer/src/plugin/`):
  - `min-ctx.ts` — 极简 cordis Context(fiber.inject / get / provide / effect / on / timeout / interval / _dispose)。
  - `slot-registry.ts` — 附加型槽白名单(ADDITIVE_SLOT_SPECS 16 槽;root/conversation/sidebar 主机位
    拒绝;tool.call.toolview 已发货 key 拒绝),register 原型方法 + spec/inject/entries/winner。
  - `evaluator.ts` — 官方 evaluateClientHalf 语义:new Function 闭包求值,React/console/styles/host/
    harness/traps 符号面,浏览器 timer/fetch/require trap,styles.insert 样式注入,host.call → 本地 stub。
  - `guard.ts` — dynamicCordisContext 白名单 Proxy:CTX_VERBS 惰性转发、声明式服务直读、ctx.get
    可选、slots 坐席(ledger + 阴影优先级 + trackSlotDispose)、Context 返回拒绝。
  - `runtime.ts` — 直拼 runner:load(clientCode) → evaluate → guard → apply(同步,替代
    loader.create→resolve→fiber.await)→ 卸载 = slot disposers + styles.dispose + ctx._dispose。
  - `anchors.tsx` — SlotAnchor(list/keyed/single 按槽 kind 渲染)+ `hub.tsx` 单例。
  - `demo.ts` + `ui/PluginHost.tsx` — 演示插件(注册 sidebar.footer.action + conversation.input.dock;
    尝试注册 root 被 guard 拒)+ 控制条(载入/禁区探针/卸载)。
- 验证(fixture,`probe-plugin.mjs`):载入演示 → sidebar badge + input.dock 渲染;再载禁区探针 →
  dock 2 条目;卸载 → DOM 归零 + host 未加载;零 JS 错误。证据 `zion-verify/plugin-runtime-*`。

## 3. 真后端(3080)全功能对等验收 + 24 项清单
- `probe-checklist.mjs`(复刻连真后端,逐项 DOM 验证):**24/24 pass**,零控制台错误。
  - M1:顶栏/侧栏/会话行≥1/搜索/工作区名/badge ✅
  - M2:对话区/输入栏/文本框/模型选择器/目录选项/附件 ✅
  - M3:工具卡 51 张(真实)/InteractionDock 插槽/附件容器/插件控制条/附加型槽锚点 ✅
  - 工作区 `dsh-plugins`、badge `CONNECTED`、模型选项 28(真实目录)。
- 队列 dock 真后端激活(`probe-queue.mjs`):选中真实运行会话「为deepseek harness封装GUI」,
  `queue dock before: true`(该会话已有排队)→ `queue rows: 待发送` 渲染(session/queue 帧
  到达 snapshot.queue → QueueDock),零错误。证据 `zion-verify/queue-real-backend.*`。

## 4. vision 像素回归(大致相似口径)
- 官方 3080 首屏 vs 复刻 5199(连真后端)hero 同态对照:`zion-verify/hero-parity/heatmap`。
  overall diff ~90%(官方当前新版为**浅色两栏**:品牌/工作区/搜索/设置嵌左栏 + 中央 hero/composer;
  复刻为**深色三栏**骨架 + 顶栏 + 右栏占位 + 插件条)。结构要素对齐:品牌行/会话列表(真实数据)/
  中央 hero「探索未至之境·预览版」字面一致。差异 = 主题色 + 布局版本差异,符合「大致相似」。
- 会话态对照(官方 hero vs 复刻选中会话)diff 88%:中央内容本质不同(hero vs 对话),侧栏接近。

## 5. 遗留
- 队列 dock 已在真后端激活;steering/context 摆放与 updateQueue 操作未端到端(需排队中撤销动作)。
- 插件运行时:host.call 无宿主面(stub);settings.plugin.item / shell.overlay / assistant-actions
  锚点未接(附加上即可);cordis_run 审批编排(wire)未做——底座按 Q17A 已够。
- 像素 1:1 精修(主题浅色化、右栏对齐官方新版两栏布局)——超出「大致相似」验收线,后续批。

---

# M5 进展(appended,round 6 — 剩余附加型槽锚点 + cordis_run 审批编排)

## 1. 剩余附加型槽锚点(全部接齐)
| 槽 | 锚点 | 验证 |
|---|---|---|
| `conversation.chat.assistant-actions`(list) | ChatView 每条 assistant/assistant-step 节点的 action 行 | ✅ 13 条 assistant 消息渲染「复制回答」按钮 |
| `settings.plugin.item`(keyed) | DetailsPanel 右栏(选中会话时) | ✅ 渲染「🧩 插件设置卡(settings.plugin.item)」 |
| `tool.call.toolview`(keyed) | ChatView tool-call 节点,按工具名 key 分派 | ✅ 按 key 匹配:无匹配 key 不渲染(fixture 无 zion_tool_demo 卡,正确行为);已发货 key(10 个官方工具)注册被拒 |
| `shell.overlay` / `sidebar.footer.action` / `conversation.input.dock` | M4 已接 | ✅ 回归通过 |

- SlotAnchor keyed 分支改为**按 ownerProps.key/tool 过滤条目**(此前渲染全部 keyed 条目,违反 keyed 语义)。

## 2. cordis_run 审批编排(页面侧)
- **remote 桥** `src/plugin/remote.ts`:用 vendor 的 `createWebConnectionRpc` 直接发官方通道
  `POST /api/dynamicCordisRunner/<method>`(runHostHalf/getClientCode/resolveRequestRun/settleUserRun),
  payload `{args}` — 零 cordis,与官方 `ctx.remote.dynamicCordisRunner.*` 同 wire 形状。
- **编排器** `src/plugin/run-orchestrator.ts`:接收 `cordis/request-run`(open → 需审批则
  awaiting-approval,预授权则直跑)/ `cordis/request-run-resolved`(close);approve →
  runHostHalf → getClientCode → runtime.load → resolveRequestRun;decline → 本地清态 +
  resolveRequestRun(rejected)。activity/error 经 observable 暴露。
- **host 帧接线** `src/protocol/assemble.ts`:`host/remote-event` 帧 → `sinks.onRemoteEvent`;
  RuntimeProvider 把它泵给 `getPluginRuntimeHandle().handleRemoteEvent`(hub 非 hook 出口)。
- **审批 UI** PluginHost:awaiting-approval 卡(名称/mode/用途 + 拒绝/允许);hub 暴露
  approveRun/declineRun/subscribeRuns。

## 3. 验证
- **真后端 remote 桥**:`POST /api/dynamicCordisRunner/inventory` → `{ok:true,value:[]}`
  (真实 3080,进程无动态插件,空列表正确),badge CONNECTED,零错误。
- **审批卡端到端**(注入 wire 形状 `cordis/request-run`,走真实 pump):
  卡渲染 `probe-plugin | run | probe purpose | 拒绝 | 允许` → 点拒绝 → 卡消失,零错误。
- 证据:`zion-verify/cordis-run-inventory.*`、`cordis-approval-card.*`(png/txt/errors 空)。

## 4. 遗留
- approve 全链路(runHostHalf → getClientCode → load)需真后端有实际动态插件时验证——inventory
  为空,走不到;wire 形状已按官方 remote-client 对齐。
- host.call(插件→宿主方法)仍为 stub;官方经同一 remote 通道 invoke,后续可接。




